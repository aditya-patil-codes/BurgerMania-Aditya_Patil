This project is Built using DOTNET (web apis) and on Client side React + TypeScript.

Run the following commands in backend of this app to get started
1) Add-Migration <MigrationName>
2) Update-Database

For Client side : 
1) npm i
2) npm run dev
